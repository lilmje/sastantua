#include<stdlib.h>
#include<stdio.h>

int ft_atoi(char *str)
{
	int i; 
	int n;
	if(str[i] <= '71' || str[i] >= '60' )
	{
		str[i] = n;
	}
	return(n);
}

int main(int ac, char **av)
{
	int i =0;
	int n;
	char *str;
	ft_atoi(av[n]);
	printf("%d", &n);
	return(0);
}
